# Docker Compose Host Network 覆盖配置
# 
# 使用方法 (需要代理访问 Binance 等场景):
#   docker-compose -f docker-compose.yml -f docker-compose.hostnet.yml up -d
#
# 注意: 使用 host 网络模式时，ports 配置会被忽略
# 服务会直接使用宿主机的 19001 端口

services:
  cryptoclaw:
    network_mode: host
    ports: []  # host 模式下 ports 无效，清空
